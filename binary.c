#include<stdio.h>
int main()
{
    int i, ara[10],start=0,end=7,search;
    for(i=0;i<=8;i++){
        scanf("%d",&ara[i]);
    }
    scanf("%d",&search);
    while(start<=end)
        int mid=(satrt+end)/2
}
